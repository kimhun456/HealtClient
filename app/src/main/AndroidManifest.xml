<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.swmem.healthclient">

    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <uses-permission android:name="android.permission.NFC" />
    <uses-feature android:name="android.hardware.nfc" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_blood"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".BluetoothActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar"></activity>

        <provider
            android:name=".db.HealthContentProvider"
            android:authorities="org.swmem.healthclient"
            android:enabled="true"
            android:exported="true" />

        <activity
            android:name=".StartActivity"
            android:theme="@style/Theme.Design.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>

        </activity>

        <activity android:name="org.swmem.healthclient.DeviceListActivity"
            android:label="@string/select_device"
            android:theme="@android:style/Theme.Holo.Dialog.NoActionBar"
            android:configChanges="keyboardHidden|orientation|screenSize" >
        </activity>

        <activity android:name=".SettingActivity"></activity>

        <activity
            android:name=".NfcActivity"
            android:theme="@android:style/Theme.Dialog" />

        <activity
            android:name=".NfcTextActivity"/>

        <service
            android:name="org.swmem.healthclient.service.BTCTemplateService"
            android:label= "TEST"
            android:configChanges="keyboardHidden|orientation|screenSize" >
        </service>


        <receiver android:name="org.swmem.healthclient.service.ServiceMonitoring$ServiceMonitoringBR" />

    </application>

</manifest>